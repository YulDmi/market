package com.geekbrain.market.dto;

import com.geekbrain.market.entities.Product;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.springframework.data.domain.Page;

import java.util.List;
import java.util.stream.Collectors;

//@Data
//@NoArgsConstructor
public class ProductFilterDto {
//    private List<ProductDto> productDtoList;
//
//    public ProductFilterDto(Page<Product> page) {
//        this.productDtoList = page.getContent().stream().map(ProductDto::new).collect(Collectors.toList());
//           }
}
